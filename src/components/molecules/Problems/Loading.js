import React from 'react'
import { CircularProgress } from '@mui/material'

export const Loading = () => {
  return (
    <div
      style={{
        marginTop: 0,
      }}
    >
      <CircularProgress />
    </div>
  )
}
